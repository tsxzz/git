feature-styles:
  - name: 210203name
    title: 地级市、地区、自治州、盟界
    rules:
      - symbolizers:
          - text:
              label: ${Name}          # 显示数据中 Name 字段的值
              fill-color: '#d2cfcf'   # 文本填充颜色
              font-size: 12
              geometry: ${centroid(the_geom)}  # 使用多边形的中心点
              halo:
                fill-color: '#797979' # 描边颜色（黑色）
                radius: 2             # 描边半径（以像素为单位）
